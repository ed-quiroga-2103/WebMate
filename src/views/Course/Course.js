import React from "react";
function Course(){
  const base = "course";
  //QUIROGA: aquí va grafo, info de base de datos
  return (
    <main className={`${base}__root`}>
      <div className={`${base}__wrapper`}>
        
      </div>
    </main>
    );
    
}

export default Course;